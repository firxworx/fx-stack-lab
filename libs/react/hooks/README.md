# react-hooks (@firx/react-hooks)

React hooks library.

This library was generated with [Nx](https://nx.dev).

## Running unit tests

Run `pnpm nx test react-hooks` to execute the unit tests via [Jest](https://jestjs.io).
